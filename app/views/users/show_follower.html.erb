<h1>フォロワー一覧</h1>
<% @user.followers.each do |user| %>
      <tr>
        <td><h1><%= link_to  user.name,user_path(user) %></h1></td>
        <td><%= user.login_id %></td>
        <td><%= image_tag user.icon.url, width: '20%' if user.icon? %></td>
      </tr>
<% end %>